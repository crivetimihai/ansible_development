# Molecule managed
---
file:
  /usr/bin/atom:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "atom --help"
    stdout:
      - "/atom/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
