# Molecule managed
---
file:
  /usr/bin/vscode:
    exists: true
    owner: root
    group: root

command:
  help:
    exit-status: 0
    exec: "vscode --help"
    stdout:
      - "/vscode/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
